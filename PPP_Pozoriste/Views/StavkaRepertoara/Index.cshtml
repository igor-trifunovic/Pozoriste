@model IEnumerable<PPP_Pozoriste.Models.StavkaRepertoara>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DatumPrikazivanja)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Predstava.NazivPredstave)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DatumPrikazivanja)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Predstava.NazivPredstave)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StavkaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StavkaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StavkaID })
        </td>
    </tr>
}

</table>
